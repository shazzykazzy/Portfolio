"""URL configuration for sessions app"""
from django.urls import path

app_name = 'sessions'

urlpatterns = [
    # Endpoints for sessions, bookings, availability
    # Will be implemented with viewsets
]
