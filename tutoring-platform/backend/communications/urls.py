"""URL configuration for communications app"""
from django.urls import path

app_name = 'communications'

urlpatterns = [
    # Endpoints for messages, notifications, emails
    # Will be implemented with viewsets
]
