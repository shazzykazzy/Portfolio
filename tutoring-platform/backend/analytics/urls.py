"""URL configuration for analytics app"""
from django.urls import path

app_name = 'analytics'

urlpatterns = [
    # Endpoints for analytics, metrics, reports
    # Will be implemented with viewsets
]
