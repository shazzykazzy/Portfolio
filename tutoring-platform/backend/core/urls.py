"""URL configuration for core app"""
from django.urls import path

app_name = 'core'

urlpatterns = [
    # Endpoints for subjects, topics, FAQs, testimonials, site settings
    # Will be implemented with viewsets
]
