"""URL configuration for students app"""
from django.urls import path

app_name = 'students'

urlpatterns = [
    # Endpoints for student profiles, progress, achievements, goals
    # Will be implemented with viewsets
]
